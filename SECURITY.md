# Security Policy

Report suspected vulnerabilities privately to the repository owner. Do not put
API keys, OAuth credentials, database URLs, generated media, or deployment
details in a public issue.

All credentials in `.env.example` are placeholders. OpenRouter, Google OAuth,
Auth.js, and database credentials must remain server-side and must never use a
`NEXT_PUBLIC_` prefix.
